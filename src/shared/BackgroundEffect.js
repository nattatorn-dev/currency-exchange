import styled from 'styled-components'

const BackgroundEffect = styled.div`
  ${/* transition: ${ ( { transition = '.3s ease-in-out, width .3s ease-in-out' } ) => transition }; */ '' }
`

export default BackgroundEffect
